{
  "name": "@habit-gamba/wallet",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun run check-types",
    "check-types": "tsc --noEmit",
    "lint": "oxlint .",
    "test": "bun --env-file=../../.env vitest run --passWithNoTests"
  },
  "dependencies": {
    "@habit-gamba/db": "workspace:*",
    "drizzle-orm": "^0.45.1"
  }
}
